import React from 'react';
import TeddySecret from '@/components/TeddySecret';

export const metadata = {
  title: 'Secret Teddy — Yajat & Nush',
  description: 'A secret page just for Nush',
};

export default function TeddyPage() {
  return <TeddySecret />;
}
